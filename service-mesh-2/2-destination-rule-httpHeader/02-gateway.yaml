apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  labels:
    app: dr-path-rule-httpheader
  name: dr-path-rule-httpheader-gw
spec:
  selector:
    istio: ingressgateway
  servers:
  - hosts:
    - "*"
    port:
      name: http
      number: 80
      protocol: HTTP
