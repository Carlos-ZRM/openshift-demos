apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name:  gw
  labels:
    app: vs-httpretry
  namespace: vs-httpretry
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "*"
